---
interface Props {
  lang?: "en" | "id";
}

const { lang = "id" } = Astro.props;

const projects = [
  {
    title: "Evalique Skin & Beauty",
    category: lang === "en" ? "Luxury Fashion & Skincare" : "Fashion, Beauty & Skincare",
    result: lang === "en"
      ? "Online transactions surged 2x in the first 3 months"
      : "Transaksi online naik 2x dalam 3 bulan pertama",
    image: "/images/services/ecommerce/portfolio-evalique-ecom.png",
    alt: "Website Toko Online Evalique Skin & Beauty - Gilabs",
    tags: ["Midtrans QRIS", "Cek Ongkir Otomatis", "Flash Sale Ready"]
  },
  {
    title: "TopGun Apparel & Essentials",
    category: lang === "en" ? "Lifestyle & Men's Grooming" : "Lifestyle, Apparel & Grooming",
    result: lang === "en"
      ? "90% of orders processed automatically via QRIS & Virtual Account"
      : "90% pesanan terverifikasi otomatis via QRIS & VA",
    image: "/images/services/ecommerce/portfolio-topgun-ecom.png",
    alt: "Website Toko Online TopGun - Gilabs",
    tags: ["Mobile Checkout", "Automated Invoices", "Multi-Varian SKU"]
  },
  {
    title: "Savlo Digital Goods",
    category: lang === "en" ? "Digital Products & Memberships" : "Produk Digital & Membership",
    result: lang === "en"
      ? "100% automated instant delivery upon payment confirmation"
      : "Penjualan digital otomatis tanpa proses manual",
    image: "/images/services/ecommerce/portfolio-savlo-ecom.png",
    alt: "Website E-Commerce Savlo Digital - Gilabs",
    tags: ["Instant Download", "Customer Database", "Zero Commission"]
  }
];

const portfolioUrl = lang === "en" ? "/en/portfolio" : `/${lang}/portfolio`;
---

<section id="portofolio" class="py-16 md:py-24 relative">
  <div class="container mx-auto px-4 max-w-6xl">
    {/* Header */}
    <div class="max-w-3xl mx-auto text-center mb-12 md:mb-16 px-4">
      <div class="flex items-center justify-center gap-3 sm:gap-4 mb-3 md:mb-4">
        <div class="h-px w-6 sm:w-12 bg-primary/10"></div>
        <span class="typography-eyebrow text-foreground/80">
          {lang === "en" ? "Proven Stores" : "Portofolio Unggulan"}
        </span>
        <div class="h-px w-6 sm:w-12 bg-primary/10"></div>
      </div>

      <h2 class="typography-section-title mb-4">
        {lang === "en" ? "Online Stores Live & Generating Revenue" : "Toko Online yang Sudah Live dan Jualan"}
      </h2>

      <p class="typography-subtitle max-w-2xl mx-auto leading-relaxed text-foreground/80 font-light">
        {lang === "en"
          ? "Live storefronts built for independent brands that process seamless payments and generate steady revenue every single day."
          : "Toko online nyata yang aktif melayani ribuan transaksi pembeli secara otomatis tanpa hambatan konfirmasi transfer manual."}
      </p>
    </div>

    {/* Portfolio Grid - 3 Large Cards */}
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mb-12 md:mb-16">
      {projects.map((project) => (
        <article class="bg-white border border-border/50 rounded-2xl sm:rounded-3xl overflow-hidden hover:border-brand/50 transition-colors shadow-md shadow-black/5 flex flex-col justify-between group">
          {/* Large Screenshot Image Container */}
          <div class="relative aspect-[16/10] overflow-hidden bg-muted/20 border-b border-border/40">
            <img
              src={project.image}
              alt={project.alt}
              class="w-full h-full object-cover select-none"
              loading="lazy"
              decoding="async"
              width="800"
              height="500"
            />
          </div>

          {/* Card Info */}
          <div class="p-6 sm:p-7 flex flex-col grow justify-between">
            <div>
              {/* Project Title */}
              <h3 class="typography-card-title text-foreground mb-3 group-hover:text-brand transition-colors">
                {project.title}
              </h3>

              {/* Short Measurable Result */}
              <div class="inline-flex items-center px-3.5 py-1.5 rounded-xl border border-brand/20 bg-brand/5 text-brand font-medium text-xs mb-4">
                <span>{project.result}</span>
              </div>
            </div>

            {/* Tech Tags */}
            <div class="pt-4 border-t border-border/40 flex flex-wrap items-center justify-between gap-2">
              <div class="flex flex-wrap gap-1.5">
                {project.tags.map((tag) => (
                  <span class="px-2 py-0.5 text-[11px] font-mono font-medium bg-secondary/60 text-foreground/70 rounded">
                    {tag}
                  </span>
                ))}
              </div>
              <a
                href={portfolioUrl}
                class="text-xs font-semibold text-brand hover:underline cursor-pointer"
              >
                {lang === "en" ? "Details →" : "Detail →"}
              </a>
            </div>
          </div>
        </article>
      ))}
    </div>

    {/* Bottom CTA button */}
    <div class="text-center">
      <a
        href={portfolioUrl}
        class="inline-flex items-center justify-center px-8 py-3.5 sm:py-4 typography-button text-foreground bg-white hover:bg-secondary/40 border border-border/80 rounded-full transition-colors cursor-pointer shadow-sm"
      >
        <span>{lang === "en" ? "View Complete Portfolio" : "Lihat Portofolio Lengkap"}</span>
      </a>
    </div>
  </div>
</section>
