---
import WebDevHero from "./WebDevHero.astro";
import WebDevProblems from "./WebDevProblems.astro";
import WebDevSolutions from "./WebDevSolutions.astro";
import WebDevProcess from "./WebDevProcess.astro";
import WebDevPortfolio from "./WebDevPortfolio.astro";
import WebDevPricing from "./WebDevPricing.astro";
import WebDevFAQ from "./WebDevFAQ.astro";
import WebDevCTA from "./WebDevCTA.astro";
import type { ServicePageData } from "../../../data/services/types";

interface Props {
  lang: "en" | "id";
  service: ServicePageData;
}

const { lang, service } = Astro.props;
---

<div class="web-dev-service-page">
  {/* 1. HERO SECTION */}
  <WebDevHero lang={lang} service={service} />

  {/* 2. MASALAH YANG SERING DITEMUI */}
  <WebDevProblems lang={lang} service={service} />

  {/* 3. SOLUSI / VALUE PROPOSITION */}
  <WebDevSolutions lang={lang} service={service} />

  {/* 4. PROSES KERJA */}
  <WebDevProcess lang={lang} service={service} />

  {/* 5. PORTOFOLIO */}
  <WebDevPortfolio lang={lang} service={service} />

  {/* 6. PRICING */}
  <WebDevPricing lang={lang} service={service} />

  {/* 7. FAQ */}
  <WebDevFAQ lang={lang} service={service} />

  {/* 8. FINAL CTA */}
  <WebDevCTA lang={lang} service={service} />
</div>
