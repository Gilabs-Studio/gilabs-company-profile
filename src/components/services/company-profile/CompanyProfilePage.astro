---
import CompanyProfileHero from "./CompanyProfileHero.astro";
import CompanyProfileProblems from "./CompanyProfileProblems.astro";
import CompanyProfileSolutions from "./CompanyProfileSolutions.astro";
import CompanyProfileFeatures from "./CompanyProfileFeatures.astro";
import CompanyProfileFAQ from "./CompanyProfileFAQ.astro";
import CompanyProfileCTA from "./CompanyProfileCTA.astro";
import type { ServicePageData } from "../../../data/services/types";

interface Props {
  lang: "en" | "id";
  service: ServicePageData;
}

const { lang, service } = Astro.props;
---

<div class="company-profile-service-page">
  {/* 1. HERO SECTION */}
  <CompanyProfileHero lang={lang} service={service} />

  {/* 2. MASALAH YANG SERING DITEMUI */}
  <CompanyProfileProblems lang={lang} service={service} />

  {/* 3. SOLUSI / VALUE PROPOSITION */}
  <CompanyProfileSolutions lang={lang} service={service} />

  {/* 4. FITUR UTAMA */}
  <CompanyProfileFeatures lang={lang} service={service} />

  {/* 5. FAQ */}
  <CompanyProfileFAQ lang={lang} service={service} />

  {/* 6. FINAL CTA */}
  <CompanyProfileCTA lang={lang} service={service} />
</div>
